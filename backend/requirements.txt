fastapi==0.115.12
uvicorn[standard]==0.30.6
pydantic-settings==2.13.1
python-multipart==0.0.20
supabase==2.28.2
PyJWT==2.12.1
PyMuPDF==1.27.2
requests==2.32.3
psycopg[binary,pool]
pgvector==0.4.2

# Optional: only needed if you switch back to Pydantic EmailStr fields.
email-validator==2.3.0

# Local embeddings: CPU-only PyTorch (no CUDA wheels — required for Railway build size/time).
# Default pip torch pulls ~3GB+ nvidia-* packages and often hits CI timeouts.
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.6.0,<3
sentence-transformers>=3.3.0

